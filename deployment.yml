apiVersion: apps/v1
kind: Deployment
metadata:
 name: webappnew
spec:
 replicas: 4
 selector:
  matchLabels:
   app: nginx
 template:
  metadata:
   labels:
    app: nginx
  spec:
   containers:
   - name: nginx
     image: quay.io/gauravkumar9130/mywebapp
     ports:
     - containerPort: 80
